# HK & GBA | Prediction of flood related issues
_**By**_: *TL & TW & VF & FH*

## Backend 3D Terrain Viewer

```bash
uv run python main.py --backend-vis            # 50 m/pixel (default)
uv run python main.py --backend-vis --downsample 20   # 100 m/pixel (faster)
```