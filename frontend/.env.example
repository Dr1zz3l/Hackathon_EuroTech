# Get your free token at https://cesium.com/ion/ → Account → Access Tokens
# The OSM 3D Buildings tileset (Asset ID 96188) is free for all CesiumIon accounts.
# Without a token the app runs but 3D buildings are disabled.
VITE_CESIUM_TOKEN=your_cesiumion_access_token_here
