# Next.js (App Router, server build) on Netlify.
# The official Next runtime plugin wires up SSR/routing — no manual SPA redirect.
[build]
  base    = "frontend"
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"
